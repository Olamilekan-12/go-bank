# Application environment: development | staging | production
APP_ENV=development

#Port the HTTP API listens on
HTTP_PORT=8080

#Max time to wait for in-flight request on shutdown (Go duration: 30s, 1m)
SHUTDOWN_TIMEOUT=30s